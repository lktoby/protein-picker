# Web アプリ開発フロー（Claude Code）

このプロジェクトは、要件定義から出荷判定までを **5段階のカスタムコマンド** で順番に進めます。
各段階の成果物は `docs/` 配下に蓄積され、次の段階の入力になります。

## 開発フローの全体像

```
/requirements      要件定義（壁打ち → ユーザーストーリー・機能一覧）   … 人間承認 必須 ●
   ↓
/prototype         デモ作成（モックで動く画面）→ 関係者レビュー         … 人間レビュー 必須 ●
   ↓                    ├─ 要件の漏れが見つかったら /requirements に戻る
   ↓                    └─ UI/UX の指摘は ui-spec.md に確定させ、後工程が踏襲する
/design            詳細設計（DB設計・API仕様・画面フロー）             … AI自走 ○
   ↓
/implement         実装・単体テスト                                    … AI自走 ○
   ↓
/acceptance-test   受け入れテストシナリオ作成 → 人間が出荷判定          … 人間チェック 必須 ●
```

- `●` … **人間の承認・判断が必須のゲート**。ここは AI が勝手に先へ進まない。
- `○` … **AI が承認なしで自走してよい工程**。

要件定義（`/requirements`）では、まず **ユーザーストーリー** を固めて人間に確認を取り、その後に **機能一覧** を作る。
ここで扱うのは「何を作るか（What）」まで。技術スタック・DB・API・画面などの「どう作るか（How）」には踏み込まず、後続工程に委ねる。

デモ作成・レビュー（`/prototype`）では、承認済み要件を **モックデータで動くフロントエンドのデモ** にして関係者に見せ、
**設計・実装が始まる前に要件の漏れ・認識ズレを潰す**（手戻り防止）。DB・API・認証は作らない、捨てる前提のコード。
レビューで見つかった要件の漏れは、設計・実装ではなく **要件定義に戻して直し、再承認する**。

**この工程は、要件の漏れだけでなく UI/UX（画面構成・操作方法）の指摘も取り込む。**
「何を作るか（What）」は要件定義で決まっているが、「その画面でどう操作するか」は動く画面を見て初めて判断できる。
例：「ステータスはドラッグ＆ドロップで動かしたい」「この情報も一覧に出したい」。
こうした指摘は要件定義に戻さず、**この工程で確定させて `docs/02-prototype/ui-spec.md`（UI/UX 仕様）に書き残し、
`/design`・`/implement` がそれを踏襲する。** デモのコードは捨てるが、**レビューで合意した UI/UX は捨てない。**

実装は **テスト駆動開発（TDD）** で進める。
単体テストのテストケース（テスト観点）は **実装工程（`/implement`）で洗い出し**、
「テストケース → テストコード（先に書く／Red）→ 実装（通す／Green）→ リファクタ」の順で進める。
最終的な受け入れテストは `/acceptance-test` で作成する。

## 成果物の置き場所

| 段階 | ディレクトリ | 主な成果物 |
|------|-------------|-----------|
| 要件定義 | `docs/01-requirements/` | `user-stories.md`, `feature-list.md` |
| デモ・レビュー | `docs/02-prototype/` | `prototype-notes.md`, `review-feedback.md`, `ui-spec.md`（後工程が踏襲する UI/UX 仕様）。デモのコードは `src/app/prototype/` |
| 詳細設計 | `docs/03-design/` | `db-design.md`, `api-spec.md`, `screen-flow.md` |
| 実装 | `docs/04-implementation/` | 実装メモ、`impl-notes.md`。コード本体はプロジェクト構成に従う |
| 受け入れ | `docs/05-acceptance/` | `acceptance-scenarios.md`（人間がチェックする出荷判定シート） |

どの工程でも、AI からユーザーへの質問は各ディレクトリの `questions.md`（質問シート）に書き出す。後述の「ユーザーへの確認は『質問シート』で行う」を参照。

## 動作確認・起動方法（重要）

アプリの起動・画面確認は **Docker（`docker compose up --build`）で行う。** ローカル環境に Node.js/npm を直接インストールして `npm run dev` や `npm start` を実行しない。

- 起動: `docker compose up --build` → http://localhost:3000 で確認
- 停止: `docker compose down`
- 詳しい手順・トラブルシューティングは `README.md` と `onboarding/docker_setup.md` を参照。

`/prototype` で関係者にデモを見せるとき、`/implement` で実装後の画面を確認するときなど、**ブラウザで実際に画面を開いて確認する場面ではすべて Docker 経由で起動する。**
起動前にポート `3000` を他のプロジェクトが使っていないか確認する（例: `lsof -nP -iTCP:3000 -sTCP:LISTEN`）。他プロセスが使用中だとコンテナのポート転送が正しく機能せず、無関係な画面が表示されることがある。

単体テスト（`npm test` / `npm run test:watch`）はローカル実行でよい。

## 承認ゲートの運用ルール（重要）

これらは AI の既定動作を上書きする、このプロジェクトの絶対ルールです。

1. **要件定義（`/requirements`）は人間承認なしに次工程へ進めてはならない。**
   - `docs/01-requirements/` のドキュメントに、人間の承認記録（承認日・承認者）が書かれていることを `/prototype`・`/design` 開始時に確認する。
   - 承認記録がなければ、デモ・設計・実装を始めず、承認を求めて停止する。

2. **デモレビュー（`/prototype`）は人間のレビュー実施なしに次工程へ進めてはならない。**
   - `docs/02-prototype/review-feedback.md` にレビュー記録（実施日・参加者・次工程へ進んでよいかの判断）が書かれていることを `/design` 開始時に確認する。
   - レビュー結果を AI が勝手に「レビュー済み」「進んでよい」と記入しない。
   - レビューで要件の漏れが見つかったら、設計・実装で辻褄を合わせず、**要件定義に戻して直し再承認を得る。**
   - **UI/UX（操作方法・画面構成）の指摘は「後工程で決めること」として先送りしない。** デモに反映して合意を取り、`docs/02-prototype/ui-spec.md` に確定仕様として書き残す。

3. **詳細設計・実装・単体テスト（`/design`, `/implement`）は AI が承認を求めずに進めてよい。**
   - ただし要件定義の範囲を超える新機能の追加はしない。範囲外の判断が必要になったら `/requirements` に立ち返る。
   - **`docs/02-prototype/ui-spec.md` の UI/UX 仕様は必須の入力であり、勝手に変えない。** 変える必要が生じたら理由を成果物に明記する。レビュー指摘ID（RV-xx）は設計・実装の成果物まで追跡できるようにする。

4. **出荷判定は必ず人間が行う。**
   - `/acceptance-test` は受け入れテストシナリオを作るところまで。合否判定・出荷可否は人間が決める。AI は「出荷可能」と結論づけない。

## ユーザーへの確認は「質問シート（questions.md）」で行う（重要）

これも AI の既定動作を上書きする、このプロジェクトの絶対ルールです。

**AI がユーザーに確認・判断を求めるときは、チャット上で質問して回答を待つのではなく、
その工程のディレクトリにある `questions.md`（質問シート）に質問を書き出し、ユーザーが同じファイルに回答を記入する。**

理由：質問と回答が成果物として `docs/` に残り、後から「なぜこの仕様になったか」を追跡できる。
インターンが落ち着いて考えてから回答でき、関係者に確認してから答えることもできる。

### 質問シートの置き場所

| 工程 | 質問シート |
|------|-----------|
| 要件定義 | `docs/01-requirements/questions.md` |
| デモ・レビュー | `docs/02-prototype/questions.md` |
| 詳細設計 | `docs/03-design/questions.md` |
| 実装 | `docs/04-implementation/questions.md` |
| 受け入れ | `docs/05-acceptance/questions.md` |

### 運用手順

1. **AI：質問を書き出す。** 確認したいことを、その時点でまとめて質問シートに追記する（1問ずつ小出しにしない）。
   - 既存の質問シートがあれば、**新しいラウンドの見出しを付けて追記する。過去の質問・回答は消さない。**
2. **AI：停止して依頼する。** チャットには「`docs/xx/questions.md` に質問を N 件書き出しました。回答を記入したら教えてください」とだけ伝え、**そこで一旦作業を止める。**
   - 質問の中身をチャットに全文コピーしない（シートを見てもらう）。
3. **ユーザー：質問シートの「回答」欄に記入する。**
4. **AI：再開時に質問シートを読み直す。** 回答済みの内容を成果物に反映し、各質問のステータスを `回答済み` に更新する。
   - 未回答・回答が曖昧なものが残っていたら、勝手に決めずに **追加質問として同じシートに書き足し、再度停止する。**

### 質問の書式（テンプレート）

```markdown
### Q-01 （質問のタイトル）
- ステータス: 未回答
- なぜ聞くか: この回答がどの成果物のどこに効くか
- 選択肢:
  - A) ...
  - B) ...
  - C) その他（自由記述）
- AI の推奨: A（理由）

**回答:**
> ここに回答を記入してください
```

### 制約

- **AI が回答欄を自分で埋めない。** ステータスを勝手に `回答済み` にしない。
- **回答が無いまま「たぶんこうだろう」で先へ進まない。** ただし回答に依存しない作業は先に進めてよい。
- 回答が「おまかせ」「AI の推奨で」の場合は、AI が決めた内容を回答欄の下に `（AI 判断: 〜）` として明記する。
- `AskUserQuestion` ツールでチャット上の選択肢を出す運用は、このプロジェクトでは使わない。

## 各コマンドの入出力

| コマンド | 入力（前提） | 出力 |
|---------|-------------|------|
| `/requirements` | ユーザーとの対話 | `docs/01-requirements/*`（ユーザーストーリー → 機能一覧）＋ 承認欄 |
| `/prototype` | 承認済みの要件定義 | モックで動くデモ（`src/app/prototype/*`）＋ `docs/02-prototype/*`（デモ説明・レビュー記録・**UI/UX 仕様**） |
| `/design` | 承認済みの要件定義 ＋ デモレビュー完了（`docs/02-prototype/ui-spec.md`, `review-feedback.md`） | `docs/03-design/*`（RV-xx の反映先を明記） |
| `/implement` | 承認済みの要件定義 ＋ 詳細設計 ＋ `docs/02-prototype/ui-spec.md` | テストケースの洗い出し ＋ テストコード（先に作成）＋ ソースコード ＋ `docs/04-implementation/*` |
| `/acceptance-test` | 実装物 | `docs/05-acceptance/acceptance-scenarios.md` |

## コーディング / ドキュメント方針

- ドキュメント・返答は日本語で書く。
- 英字の略語・専門用語は `日本語（英語）` 形式で書く（例: `本人確認（KYC）`）。
- 最新のライブラリ仕様が必要なときはウェブ調査・ドキュメント参照を行う。
